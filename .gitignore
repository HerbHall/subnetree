# Go binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
/bin/
/subnetree
/scout

# Go test binary
*.test

# Go coverage
*.out
coverage.html

# Go vendor (if used)
/vendor/

# IDE
.idea/
.vscode/mcp.json
*.swp
*.swo
*~

# Claude Code local settings (user-specific, modified every session)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment
.env
.env.local
.env.*.local

# Data directory
/data/
*.db
*.db-shm
*.db-wal

# Working files
whats-next.md
.planning/

# Local-only coordination and research (never pushed to GitHub)
.coordination/
research/

# Node.js (web frontend)
web/node_modules/
web/dist/
web/.env
web/coverage/
web/test-results/
web/playwright-report/
web/blob-report/
web/e2e/.auth/
*.tsbuildinfo

# Embedded dashboard build (copied from web/dist/)
internal/dashboard/dist/
!internal/dashboard/dist/.gitkeep

# Generated protobuf (committed for CI; regenerate with `make proto`)

# Build output
/dist/

# Hugo (website)
website/public/
website/resources/
website/.hugo_build.lock
